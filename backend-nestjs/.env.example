# ==============================================
# SISTEMA FACTURADOR - BACKEND CONFIGURATION
# ==============================================

# ----------------------------------------------
# Database Configuration
# ----------------------------------------------
# Para desarrollo local, usa las variables individuales
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=facturador
DATABASE_PASSWORD=password
DATABASE_NAME=facturador_db

# Para producción (Render), usa DATABASE_URL directamente
# Render proporciona esta variable automáticamente
# Formato: postgresql://user:password@host:port/database
# DATABASE_URL=postgresql://user:password@host:port/dbname

# ----------------------------------------------
# Application
# ----------------------------------------------
NODE_ENV=development
PORT=3001

# ----------------------------------------------
# Security
# ----------------------------------------------
# IMPORTANTE: Generar una clave segura aleatoria para producción
# Ejemplo: usar comando: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ----------------------------------------------
# CORS - Allowed Origins
# ----------------------------------------------
# Separar múltiples orígenes con comas
# Ejemplo: https://mi-app.onrender.com,https://otro-dominio.com
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ----------------------------------------------
# Redis (Opcional - para colas)
# ----------------------------------------------
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# ----------------------------------------------
# Email Configuration (Opcional)
# ----------------------------------------------
# Para notificaciones y envío de facturas por correo
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu-email@gmail.com
SMTP_PASS=tu-app-password
EMAIL_FROM=tu-email@gmail.com

# ----------------------------------------------
# SRI Configuration (Ecuador)
# ----------------------------------------------
# Ambiente de facturación electrónica
# PRODUCCION | PRUEBAS
SRI_AMBIENTE=PRUEBAS

# Timeout para peticiones a SRI (milisegundos)
SRI_TIMEOUT=30000

# URLs de servicios SRI (se configuran automáticamente según ambiente)
# SRI_WS_AUTORIZACION=https://cel.sri.gob.ec/comprobantes-electronicos-ws/AutorizacionComprobantesOffline
# SRI_WS_RECEPCION=https://cel.sri.gob.ec/comprobantes-electronicos-ws/RecepcionComprobantesOffline

# ----------------------------------------------
# Firebase Admin (Si se usa)
# ----------------------------------------------
# USE_FIRESTORE=false
# FIREBASE_PROJECT_ID=
# FIREBASE_PRIVATE_KEY=
# FIREBASE_CLIENT_EMAIL=

# ----------------------------------------------
# File Storage
# ----------------------------------------------
# Ruta para almacenar archivos (certificados, XMLs, PDFs)
UPLOAD_PATH=./uploads

# ----------------------------------------------
# Client Path (Para servir frontend desde backend)
# ----------------------------------------------
# Ruta al build del cliente (si se sirve desde el mismo servidor)
# CLIENT_PATH=../client/dist
